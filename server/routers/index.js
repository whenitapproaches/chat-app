const userRouter = require("./user.router")
const messageRouter = require("./message.router")
const profileRouter = require("./profile.router")

module.exports = {
  userRouter, messageRouter, profileRouter
}
