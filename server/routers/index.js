const userRouter = require("./user.router")
const messageRouter = require("./message.router")
const imageRouter = require("./image.router")

module.exports = {
  userRouter, messageRouter, imageRouter
}
